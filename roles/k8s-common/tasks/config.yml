#  -*- coding:utf-8 mode:yaml -*-
#  Ansible playbook
#  Copyright 2020 Takeharu KATO All Rights Reserved.
#
#  設定関連タスク
#

# Containerdの設定

# overlay, br_netfilter モジュールのロード
- name: Load overlay, br_netfilter modules
  ansible.builtin.shell: |
    modprobe overlay
    modprobe br_netfilter
  become: true
  register: _result
  changed_when: _result.rc == 0


# moduleの設定
- name: Setup modules /etc/modules-load.d/k8s.conf
  ansible.builtin.template:
    src: modules-k8s.conf.j2
    dest: /etc/modules-load.d/k8s.conf
    owner: root
    group: root
    mode: '0644'
    backup: false

# IPv4/IPv6フォワーディングを有効化
- name: Setup containerd /etc/sysctl.d/99-k8s-cri.conf
  ansible.builtin.template:
    src: 99-k8s-cri.conf.j2
    dest: /etc/sysctl.d/99-k8s-cri.conf
    owner: root
    group: root
    mode: '0644'
    backup: false
  notify: "apply_sysctl"

- name: Apply sysctl immediately
  ansible.builtin.command: sysctl --system
  become: true
  register: _result
  changed_when: _result.rc == 0


#
# GRUB 設定の更新 (Debian 系)
#
- name: "Config Grub Debian"
  ansible.builtin.include_tasks: config-grub-debian.yml
  when:
    - ansible_facts.os_family == 'Debian'

#
# GRUB 設定の更新 (RHEL 系)
#
- name: "Config Grub Rhel"
  ansible.builtin.include_tasks: config-grub-rhel.yml
  when:
    - ansible_facts.os_family == 'RedHat'

#
# containerd の設定
#
- name: Create /etc/systemd/system/containerd.service.d
  ansible.builtin.file:
    path: "/etc/systemd/system/containerd.service.d"
    state: directory
    owner: "root"
    group: "root"
    mode: "755"

- name: Create /etc/containerd directory
  ansible.builtin.file:
    path: "/etc/containerd"
    state: directory
    owner: "root"
    group: "root"
    mode: "755"

- name: Rm /etc/containerd/config.toml
  ansible.builtin.file:
    path: "/etc/containerd/config.toml"
    state: absent
  become: true

# Containerd の設定ファイルを生成
- name: "Generate containerd config"
  ansible.builtin.shell: |
    set -o pipefail
    containerd config default | sudo tee /etc/containerd/config.toml
  args:
    executable: /bin/bash
  become: true
  register: _result
  changed_when: _result.rc == 0

# Containerd 側 Cgroup ドライバーとしてSystemdを選択
- name: "Set the systemd driver as containerd cgroup driver"
  ansible.builtin.lineinfile:
    path: /etc/containerd/config.toml
    state: "present"
    backrefs: "yes"
    regexp: '^\s*SystemdCgroup\s*=\s*false\s*$'
    line: 'SystemdCgroup = true'
  become: true

# Containerd 側のコンテナレジストリ設定
# containerd のデフォルト設定には複数の config_path が存在するため,
# sed の範囲指定で .registry セクション配下の config_path のみを置換する
#
# 留意事項:
#   lineinfile では最後にマッチした行が置換されるため正確なセクション指定ができない。
#   sed の範囲指定機能 (/pattern1/,/pattern2/) を使用して .registry セクション内のみを対象にする
#
#   sed の正規表現の意味は以下の通り:
#
#   [pattern1(開始パターン)]
#                  \[plugins\.[^]]*\.registry\] は,
#                  plugins.<任意の文字列>.registry のセクションを正規表現で指定する
#                  補足: [^]]* は, ] 以外の任意の文字列へのマッチを意味する。
#                         \[plugins\.[^]]*\.registry\] は,
#                            - '[plugins.' で始まり,
#                            - ']' 以外の任意の文字列が続き,
#                            - '.registry]' で終わる
#                         にマッチする正規表現である。
#                  例: [plugins.cri.registry] や [plugins."io.containerd.grpc.v1.cri".registry] にマッチする
#
#   [pattern2(終了パターン)]
#                  /^\s*\[/ は, 次のセクションの開始行 (行頭にゼロ個以上の空白があり, [で始まる行) を指定する
#                  例: [plugins.cri.containerd.runtimes.runc] のような行にマッチする
#
#   [置換コマンド]
#                  s|^\(\s*\)config_path\s*=.*|\1config_path = "/etc/containerd/certs.d"|
#                  - ^(\s*) は, 行頭のゼロ個以上の空白をキャプチャする(後続の \1 で参照するため)
#                  - config_path\s*=.* は, config_path = 以降の文字列
#                  - \1config_path = "/etc/containerd/certs.d" は,
#                    キャプチャした空白を保持しつつ置換後の文字列である"/etc/containerd/certs.d"を指定する
#                  - 置換後の文字列に'/'が含まれるため, sed の区切り文字を '/' ではなく '|' に変更している
#
- name: "Set containerd registry config path"
  ansible.builtin.shell: |
    set -euo pipefail
    # 修正対象ファイル
    target='/etc/containerd/config.toml'
    # 置換後の行
    new_line='      config_path = "/etc/containerd/certs.d"'
    # .registry セクション内の config_path を置換 (次のセクション [] までの範囲)
    sed -i "/\[plugins\.[^]]*\.registry\]/,/^\s*\[/{
      s|^\(\s*\)config_path\s*=.*|\1config_path = \"/etc/containerd/certs.d\"|
    }" "${target}"
  args:
    executable: /bin/bash
  become: true
  register: _result
  changed_when: _result.rc == 0

# /etc/containerd/certs.dを作成
- name: "Create /etc/containerd/certs.d"
  ansible.builtin.file:
    path: "/etc/containerd/certs.d"
    state: directory
    owner: "root"
    group: "root"
    mode: "0755"
  become: true

# コンテナレジストリのエンドポイント設定を検証する
- name: "Validate containerd registry endpoint entries"
  ansible.builtin.assert:
    that:
      - item is mapping
      - item.endpoint is defined
      - (item.endpoint | string | length) > 0
      - (item.scheme | default('', true) | lower) in ['', 'http', 'https']
    fail_msg: "Specify each element of `k8s_containerd_registry_endpoints` as a dictionary containing `endpoint`, an optional `scheme` (http/https), and `skip_verify`."
  loop: "{{ k8s_containerd_registry_endpoints | default([], true) }}"

# /etc/containerd/certs.d/<endpoint>ディレクトリを作成する
# 留意事項: <endpoint>は, コンテナレジストリのエンドポイント(<ホスト名またはIPアドレス>:<ポート>)
- name: "Create containerd registry endpoint directories"
  ansible.builtin.file:
    path: "/etc/containerd/certs.d/{{ item.endpoint }}"
    state: directory
    owner: "root"
    group: "root"
    mode: "0755"
  loop: "{{ k8s_containerd_registry_endpoints | default([], true) }}"
  become: true

# containerd registry endpointごとにhosts.tomlを作成する
- name: "Configure containerd registry hosts.toml"
  ansible.builtin.template:
    src: containerd-registry-hosts.toml.j2
    dest: "/etc/containerd/certs.d/{{ item.endpoint }}/hosts.toml"
    owner: root
    group: root
    mode: '0644'
    backup: false
  loop: "{{ k8s_containerd_registry_endpoints | default([], true) }}"
  become: true

# containerdのsystemd override.conf (ホスト固有設定値定義) を作成する
- name: Setup containerd service
  ansible.builtin.template:
    src: containerd-override.conf.j2
    dest: /etc/systemd/system/containerd.service.d/override.conf
    owner: root
    group: root
    mode: '0644'
    backup: false

# 設定を反映するためにcontainerdを再起動する
- name: Restart containerd immediately
  ansible.builtin.systemd:
    name: containerd
    state: restarted
    enabled: true
  become: true

# マシンをリブートする
- name: Reboot host gracefully
  ansible.builtin.reboot:
    reboot_timeout: "{{ reboot_timeout_sec }}"
    msg: "Reboot triggered by role: common"
    pre_reboot_delay: 2
