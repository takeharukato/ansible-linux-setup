#
#  -*- coding:utf-8 mode:bash -*-
# This file is generated by ansible.
{# 日付の取得 #}
# last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}

#
# NTPサーバの設定
# /etc/chrony/conf.d/99-ntp-servers.conf
#
{% for srv in (external_ntp_servers_list | default([], true) | select | map('trim') | reject('equalto','') | unique) %}
# {{ srv }}
pool {{ srv }} iburst
{% endfor %}

#アクセスを許可するNTPクライアントのIPアドレス or サブネットを指定
allow {{ ntp_allow }}
