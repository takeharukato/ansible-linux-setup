#!/usr/bin/env python3
#
#  -*- coding:utf-8 mode:bash -*-
# This file is generated by ansible.
{# 日付の取得 #}
# last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}

#
# CPUの範囲を改行で区切って返却する
# 他のシェルスクリプト内部から呼ばれるスクリプト
#

import os,sys


def expand(r):

    s=set()
    for p in rng.split(','):
        if '-' in p:
            a,b=map(int,p.split('-'))
            s.update(range(a,b+1))
        elif p.strip():
            s.add(int(p))
    print('\n'.join(map(str,sorted(s))))


if __name__ == "__main__":
    if "APP_RANGE" in os.environ:
        rng=os.environ["APP_RANGE"]
    else:
        if len(sys.argv) > 1:
            rng=sys.argv[1]
    expand(rng)
