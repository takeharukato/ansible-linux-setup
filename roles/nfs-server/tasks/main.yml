#  -*- coding:utf-8 mode:yaml -*-
#  Ansible playbook
#  Copyright 2020 Takeharu KATO All Rights Reserved.
#  タスクメイン部
#  include_tasksで他のタスクを読み込む

# 最初に変数設定を読み直す ( role単独で実行可能とするため )
- name: "Load Params"
  ansible.builtin.include_tasks: load-params.yml

#
# 必須変数がすべて定義済みかつ空文字ではない場合のみ, NFS構成タスクを実行
#

- name: "Package"
  ansible.builtin.include_tasks: package.yml
  when:
    - (nfs_export_directory | default('', true) | string | trim | length) > 0
    - (dns_domain | default('', true) | string | trim | length) > 0
    - (nfs_network | default('', true) | string | trim | length) > 0

- name: "Directory"
  ansible.builtin.include_tasks: directory.yml
  when:
    - (nfs_export_directory | default('', true) | string | trim | length) > 0
    - (dns_domain | default('', true) | string | trim | length) > 0
    - (nfs_network | default('', true) | string | trim | length) > 0

- name: "User Group"
  ansible.builtin.include_tasks: user_group.yml
  when:
    - (nfs_export_directory | default('', true) | string | trim | length) > 0
    - (dns_domain | default('', true) | string | trim | length) > 0
    - (nfs_network | default('', true) | string | trim | length) > 0

- name: "Service"
  ansible.builtin.include_tasks: service.yml
  when:
    - (nfs_export_directory | default('', true) | string | trim | length) > 0
    - (dns_domain | default('', true) | string | trim | length) > 0
    - (nfs_network | default('', true) | string | trim | length) > 0

- name: "Config"
  ansible.builtin.include_tasks: config.yml
  when:
    - (nfs_export_directory | default('', true) | string | trim | length) > 0
    - (dns_domain | default('', true) | string | trim | length) > 0
    - (nfs_network | default('', true) | string | trim | length) > 0
