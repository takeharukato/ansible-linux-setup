#
#  -*- coding:utf-8 -*-
#
# /etc/nsupdate/ddns-clients.key
# Dynamic DNS update key file for clients
# クライアントが動的にDNSを更新するための鍵ファイル
# 本ファイルは以下のコマンドで生成される:
#    ddns-confgen -a hmac-sha256 -k ddns-clients -r /dev/urandom
#
# This file is generated by ansible.
{# 日付の取得 #}
# last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}
#
# This file contains the key that is used to authenticate dynamic updates
# to the DNS server.
# This file is generated by the command:
#   ddns-confgen -a hmac-sha256 -k ddns-clients -r /dev/urandom
#
# To activate this key, place the following in named.conf, and
# in a separate keyfile on the system or systems from which nsupdate
# will be run:
key "{{dns_ddns_key_name}}" {
        algorithm hmac-sha256;
        secret "{{dns_ddns_key_secret}}";
};
