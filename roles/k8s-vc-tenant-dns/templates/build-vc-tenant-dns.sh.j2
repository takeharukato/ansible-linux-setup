#!/usr/bin/env bash
# -*- coding:utf-8 mode:bash -*-
#
# vc-tenant-dns コンテナイメージを構築する
#
# This file is generated by ansible.
# last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}

set -euo pipefail

image_name="{{ k8s_vc_tenant_dns_image }}"
build_dir="{{ k8s_vc_tenant_dns_build_dir }}"
output_dir="{{ k8s_vc_tenant_dns_output_dir }}"
output_file="${output_dir}/{{ k8s_vc_tenant_dns_image_file }}"
build_network="{{ k8s_vc_tenant_dns_docker_build_network }}"

docker build --network="${build_network}" -t "${image_name}" "${build_dir}"
docker save -o "${output_file}" "${image_name}"