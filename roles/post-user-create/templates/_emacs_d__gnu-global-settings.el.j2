;;  Ansible playbook
;;  Copyright 2020 Takeharu KATO All Rights Reserved.
;; This file is generated by ansible.
{# 日付の取得 #}
;; last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}
;;

;;  Ansible playbook
;;  Copyright 2020 Takeharu KATO All Rights Reserved.
;; This file is generated by ansible.
;; last update: 2022-03-08 14:13:47 UTC
;;

;;
;;  -*- coding:utf-8 mode:emacs-lisp -*-
;;

;;
;;global
;;
(push (expand-file-name "/usr/share/gtags") load-path)
(push (expand-file-name "/usr/local/share/gtags") load-path)
(push (expand-file-name "/usr/local/global/share/gtags") load-path)
(autoload 'gtags-mode "gtags" "" t)
(setq gtags-mode-hook
      '(lambda ()
         (local-set-key "\M-t" 'gtags-find-tag)
         (local-set-key "\M-r" 'gtags-find-rtag)
         (local-set-key "\M-s" 'gtags-find-symbol)
         (local-set-key "\M-g" 'gtags-find-with-grep)
         (local-set-key "\C-t" 'gtags-pop-stack)
         ))

(add-hook 'c-mode-common-hook
          '(lambda()
             (gtags-mode 1)
             ))
