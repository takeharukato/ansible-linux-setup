#
#  -*- coding:utf-8 mode:bash -*-
# This file is generated by ansible.
{# 日付の取得 #}
# last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}

#
# NTP サーバ
#
[Time]
{% set ntp_line = (ntp_servers_list | default([], true)
                   | select | map('trim') | reject('equalto','') | unique | join(' ')) %}
{% if ntp_line %}
NTP={{ ntp_line }}
{% endif %}
