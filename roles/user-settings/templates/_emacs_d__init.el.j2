;;  -*- coding:utf-8 mode:emacs-lisp -*-
;;  Ansible playbook
;;  Copyright 2020 Takeharu KATO All Rights Reserved.
;; This file is generated by ansible.
{# 日付の取得 #}
;; last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}
;;

;;
;;  init.el
;;
(require 'package)

(setq package-archives
      '(("gnu" . "http://elpa.gnu.org/packages/")
	("melpa" . "http://melpa.org/packages/")
	("org" . "http://orgmode.org/elpa/")))
;;
;; パッケージディレクトリの追加
;;
(let ((elpa-directory (locate-user-emacs-file "./elpa")))
  (when (file-directory-p elpa-directory)
    (let ((default-directory elpa-directory))
      (add-to-list 'load-path default-directory)
      (normal-top-level-add-subdirs-to-load-path))))

;;
;; user_settingsディレクトリの追加
;;
(let ((default-directory (locate-user-emacs-file "./user_settings")))
  (add-to-list 'load-path default-directory)
    (normal-top-level-add-subdirs-to-load-path))

;; ロードパスの設定
(push (expand-file-name "~/elisp") load-path)

(load "proxy-settings.el") ;; proxy設定の読込

;;
;; 設定ファイル読み込み
;;
(load "basic-settings.el") ;; 基本設定
(load "japanese-environment.el") ;; 日本語環境設定 japanese-environment.el

;;
;; 以降の設定ファイルは, パッケージを導入作業を含め, 必要に応じて
;; 利用者側で有効化することを想定している
;;

;; (load "screen-settings.el") ;; screen-settings.el
;; (load "aspell-settings.el") ;; aspell-settings.el

;;
;; C言語
;;
;; (load "c-mode-settings.el") ;; c-mode-settings.el
;; (load "bsdc-mode-settings.el") ;; bsdc-mode-settings.el
;; (load "linux-c-mode-settings.el") ;; linux-c-mode-settings.el
;; (load "hos-c-mode-settings.el") ;; hos-c-mode-settings.el

;;
;; 開発環境
;;
;; (load "cmake-settings.el") ;; cmake-settings.el
;; (load "gnu-global-settings.el") ;; gnu-global-settings.el
;; (load "gud-settings.el") ;; gud-settings.el

;; (load "markdown-mode-settings.el") ;; markdown-mode-settings.el
;; (load "yaml-mode-settings.el") ;; yaml-mode-settings.el

;; (load "dockerfile-mode-settings.el") ;; dockerfile-mode-settings.el
;; (load "docker-compose-mode-settings.el") ;; docker-compose-mode-settings.el
;; (load "docker-tramp-mode-settings.el") ;; docker-tramp-mode-settings.el

;;
;; C#
;;
;; (load "c-sharp-mode-settings.el") ;; c-sharp-mode-settings.el

;;
;; python
;;
;; (load "python3-settings.el") ;; python3-settings.el

;;
;; Rust
;;
;; (load "rust-settings.el") ;; rust-settings.el

;;
;; latex
;;

;;
;; (load "auctex-mode-settings.el") ;; auctex-mode-settings.el
;; (load "yatex-mode-setting.el") ;; yatex-mode-setting.el
