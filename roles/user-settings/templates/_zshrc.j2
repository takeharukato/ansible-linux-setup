#
#  -*- coding:utf-8; mode:bash -*-
# This file is generated by ansible.
{# 日付の取得 #}
# last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}
#
# .zshrc
#

#
# セーフパスの設定
#
SAFE_PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin


#
# Ubuntu環境でも補完が効くようにRHEL系のfpath設定に対応
#
if [ -d /usr/share/zsh/site-functions ]; then
    if (( ${fpath[(Ie)/usr/share/zsh/site-functions]} == 0 )); then
      fpath=(/usr/share/zsh/site-functions $fpath)
    fi
fi

#
# /usr/localにインストールしたパッケージに対して補完が効くようにfpath設定に対応
#
if [ -d /usr/local/share/zsh/site-functions ]; then
    if (( ${fpath[(Ie)/usr/local/share/zsh/site-functions]} == 0 )); then
        fpath=(/usr/local/share/zsh/site-functions $fpath)
    fi
fi

# The following lines were added by compinstall
zstyle :compinstall filename "${HOME}/.zshrc"

autoload -Uz compinit
compinit
# End of lines added by compinstall

#
# load common .zshrc configuration file after SAFE_PATH is set
#
[ -f ~/.zshrc.common ] && source ~/.zshrc.common

# fpath更新に備えて再設定する
compinit

#
# Lmodの設定
#
if [ -f /etc/profile.d/lmod.sh ]; then
   source /etc/profile.d/lmod.sh
   module use ${HOME}/Modules
fi

#
# load user .zshrc.proxy configuration file
#
[ -f ~/.zshrc.proxy ] && source ~/.zshrc.proxy

#
# load user .zshrc configuration file
#
[ -f ~/.zshrc.mine ] && source ~/.zshrc.mine

# The following lines were added by compinstall
zstyle :compinstall filename "${HOME}/.zshrc"

autoload -Uz compinit
compinit
# End of lines added by compinstall
