#
#  -*- coding:utf-8 mode:bash -*-
# This file is generated by ansible.
{# 日付の取得 #}
# last update: {{ '%Y-%m-%d %H:%M:%S %Z' | strftime(ansible_date_time.epoch) }}
#
# .zshrc.mine.sample
#
# このファイルは個人用 zsh 設定のサンプルです。
# 必要に応じて以下の手順でコピーしてください。
#   cp ~/.zshrc.mine.sample ~/.zshrc.mine
#   ${EDITOR:-vi} ~/.zshrc.mine
#
# ~/.zshrc.mine はバージョン管理および Ansible 展開対象外のため、
# 各ユーザが自由にカスタマイズできます。
#

# ------------------------------------------------
# ヒストリとヒストリサイズ
# 共通部でも同設定は行っているが設定を
# 変更する場合はここに記述する
# ------------------------------------------------
# HISTFILE=~/.histfile
# HISTSIZE=100000
# SAVEHIST=100000


# -------------------------------------------------
# シェル動作のカスタマイズ
# -------------------------------------------------

# C-s, C-qを無効にする。
# 共通部で設定済み
# setopt no_flow_control

# リダイレクトで上書きする事を許可しない。
# 共通部で設定済み
# setopt no_clobber

# ベルを鳴らさない。
# 共通部で設定済み
# setopt no_beep

# ^Dでログアウトしないようにする。
# 共通部では設定していないが、
# 有効にする場合はコメントアウトを外す
# setopt ignore_eof

# cdでpushdする。
# 共通部で設定済み
# setopt auto_pushd

# pushdで同じディレクトリを重複してpushしない。
# 共通部で設定済み
# setopt pushd_ignore_dups

# globを拡張する。
# 共通部で設定済み
# setopt extended_glob

# pushd を引数無しで実行した時に pushd ~ とする。
# 共通部で設定済み
# setopt pushd_to_home

# ディレクトリ名だけで移動できる。
# 共通部で設定済み
# setopt auto_cd

# rm * を実行する前に確認される。
# 共通部で確認しないよう設定済み
# 有効にする場合は以下のコメントアウトを外す
# setopt rmstar_wait

# コマンドラインの最後のスラッシュを自動的に削除しない。
# 共通部で設定済み
# setopt noautoremoveslash

# ------------------------------------------------
# プロンプト再設定
# 共通部でも同設定は行っているが設定を
# 変更する場合はここに記述する
# ------------------------------------------------
# RPROMPT=""
# PROMPT="[%n@%m %c] [%h] "
#プロンプトの自動改行を抑制する
# unsetopt promptcr

# プロンプトのカラー表示を有効
# autoload -U colors
# colors

# エスケープシーケンスを使う場合はコメントアウトを外す
# setopt prompt_subst

# ------------------------------------------------
# コマンド補完
# ------------------------------------------------

# -------------------------------------------------
# コマンドエイリアスの設定
# 以下を使用する場合は, コメントアウトを外す
# -------------------------------------------------

#
# コマンドエイリアスの設定例
#

# -------------------------------------
# where コマンド
# 共通部でも同設定は行っているが設定を
# 変更する場合はここに記述する
# -------------------------------------

# where コマンド
# 共通部でも同設定は行っているが設定を
# 変更する場合はここに記述する
# alias where="command -v"

# su コマンド
# 共通部でも同設定は行っているが設定を
# 変更する場合はここに記述する
# alias su="su -l"

# jobs コマンド
# 共通部では設定していない
# 使用する場合はコメントアウトを外す
# alias j="jobs -l"

# --------------------------------------------------
# ls エイリアス
# 共通部でも同設定は行っているが設定を
# 変更する場合はここに記述する
# --------------------------------------------------
# alias ll='ls -lh'
# alias la="ls -a"
# alias lf="ls -F"
# alias ll="ls -l"

# ------------------------------------------------
# du/df エイリアス例
# 共通部でも同設定は行っているが設定を
# 変更する場合はここに記述する
# ------------------------------------------------
# alias du="du -h"
# alias df="df -h"


# function mkcd() { mkdir -p "$1" && cd "$1"; }

#
# ここから下に個人のエイリアスや関数を追加してください
#

# ------------------------------------------------
# ls 設定
# ------------------------------------------------

# -------------------------------------
# lsコマンドのカラー表示設定
# 共通部でも同設定は行っているが設定を
# 変更する場合はここに記述する
# -------------------------------------
case "${OSTYPE}" in
    freebsd*|darwin*)
        alias ls="ls -G -w"
        ;;
    linux*)
        alias ls="ls --color"
        ;;
esac

# ------------------------------------------------
# Lmod / Environment Modules を利用する場合の設定
# 共通部でも同設定は行っているが設定を変更する
# 場合はここに記述する
# ------------------------------------------------
# ホームディレクトリに Modules ディレクトリが存在する場合にのみ有効化
# if [ -f /etc/profile.d/lmod.sh -a -d "${HOME}/Modules" ]; then
#    source /etc/profile.d/lmod.sh
#    module use "${HOME}/Modules"
# fi

# ------------------------------------------------
# Rust (cargo) 環境を利用する場合の設定
# 共通部でも同設定は行っているが設定を変更する
# 場合はここに記述する
# ------------------------------------------------
# if [ -f "${HOME}/.cargo/env" ]; then
#     source "${HOME}/.cargo/env"
# fi

# ------------------------------------------------
# 個人用 bin ディレクトリを PATH に追加する
# 共通部でも同設定は行っているが設定を変更する
# 場合はここに記述する
# ------------------------------------------------
# if [ -d "${HOME}/bin" ]; then
#     PATH="${HOME}/bin:${PATH}"
#     export PATH
# fi