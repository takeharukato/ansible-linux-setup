#  -*- coding:utf-8 mode:yaml -*-
#  Ansible playbook
#  Copyright 2020 Takeharu KATO All Rights Reserved.
#
#  タスクメイン部
#
#  include_tasksで他のタスクを読み込む
#

# 最初に変数設定を読み直す ( role単独で実行可能とするため )
- name: "Load Params"
  ansible.builtin.include_tasks: load-params.yml

# LDAP設定タスク実行条件:
# - (典型的な場合, host_varsで)ldap_enabled変数がtrueに設定され, かつ,
# - ldap_organization, ldap_domain, ldap_admin_password が
#   空文字列ではない文字列で設定され, かつ,
# - ldap_admin_port が0より大きい場合

- name: "Package"
  ansible.builtin.include_tasks: package.yml
  when:
    - ldap_enabled | default(false) | bool
    - ldap_organization | default('', true) | length > 0
    - ldap_domain | default('', true) | length > 0
    - ldap_admin_password | default('', true) | length > 0
    - ldap_admin_port | default(0, true) | int > 0

- name: "User Group"
  ansible.builtin.include_tasks: user_group.yml
  when:
    - ldap_enabled | default(false) | bool
    - ldap_organization | default('', true) | length > 0
    - ldap_domain | default('', true) | length > 0
    - ldap_admin_password | default('', true) | length > 0
    - ldap_admin_port | default(0, true) | int > 0

- name: "Directory"
  ansible.builtin.include_tasks: directory.yml
  when:
    - ldap_enabled | default(false) | bool
    - ldap_organization | default('', true) | length > 0
    - ldap_domain | default('', true) | length > 0
    - ldap_admin_password | default('', true) | length > 0
    - ldap_admin_port | default(0, true) | int > 0

- name: "Sysctl"
  ansible.builtin.include_tasks: sysctl.yml
  when:
    - ldap_enabled | default(false) | bool
    - ldap_organization | default('', true) | length > 0
    - ldap_domain | default('', true) | length > 0
    - ldap_admin_password | default('', true) | length > 0
    - ldap_admin_port | default(0, true) | int > 0

- name: "Service"
  ansible.builtin.include_tasks: service.yml
  when:
    - ldap_enabled | default(false) | bool
    - ldap_organization | default('', true) | length > 0
    - ldap_domain | default('', true) | length > 0
    - ldap_admin_password | default('', true) | length > 0
    - ldap_admin_port | default(0, true) | int > 0

- name: "Config"
  ansible.builtin.include_tasks: config.yml
  when:
    - ldap_enabled | default(false) | bool
    - ldap_organization | default('', true) | length > 0
    - ldap_domain | default('', true) | length > 0
    - ldap_admin_password | default('', true) | length > 0
    - ldap_admin_port | default(0, true) | int > 0
